import React from "react";
import styled, { css } from "styled-components";
const StyledWrapper = styled.div`
  margin-bottom: ${p => p.marginBottom || p.theme.lenMd1};
`;
const titleLg = css``;
const StyledLabel = styled.div`
  color: ${p => p.theme.colorLabel};
`;
const StyledFigure = styled.div``;

const Wrapper = ({
  figure,
  label,
  marginBottom,
  size = "md"
}) => {
  return /*#__PURE__*/React.createElement(StyledWrapper, null, /*#__PURE__*/React.createElement(StyledLabel, null, label), /*#__PURE__*/React.createElement(StyledFigure, null, figure));
};

export default Wrapper;